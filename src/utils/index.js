import { cookie } from './cookie'
import { func } from './func'
import { log } from './log'
import { network } from './network'
import { object } from './object'

/**
 * 工具包
 */
export const utils = {
  cookie,
  func,
  log,
  network,
  object,
}
